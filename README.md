# Aadi-
Cinema Booking System This is a lightweight Python application designed to automate the movie ticket booking process via a Command Line Interface (CLI). Unlike transient scripts, this system features full data persistence, meaning booking records are saved locally and reloaded upon restart.
